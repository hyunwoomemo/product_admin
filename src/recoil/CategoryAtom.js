import { atom } from 'recoil';

export const currentMajorCategory = atom({
  key: 'currentMajorCategory',
  default: null,
})

export const currentMiddleCategory = atom({
  key: 'currentMiddleCategory',
  default: null,
})
